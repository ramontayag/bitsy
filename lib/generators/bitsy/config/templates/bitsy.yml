defaults: &defaults
  bitcoind:
    host: "localhost"
    username: "admin1"
    password: "123"
    port: "19001"
    ssl: false

development:
  <<: *defaults

test:
  <<: *defaults

staging:
  <<: *defaults

production:
  <<: *defaults
